{
  "name": "frontend",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "watch": "bun build ./src/**/*.ts --outdir ./dist --target browser --watch",
    "serve": "bun run ./index.ts"
  }
}
